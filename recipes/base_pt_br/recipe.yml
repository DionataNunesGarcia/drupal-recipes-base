name: "Base PT-BR"
description: "Configura português como idioma padrão"
type: "Site"

install:
  - base_i18n

config:
  actions:
    system.site:
      simple_config_update:
        default_langcode: pt-br
        langcode: pt-br
