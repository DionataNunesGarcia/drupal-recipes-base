diff --git a/src/Service/IconResolver.php b/src/Service/IconResolver.php
index 2aa7169..b0e841b 100644
--- a/src/Service/IconResolver.php
+++ b/src/Service/IconResolver.php
@@ -92,8 +92,16 @@ class IconResolver implements IconResolverInterface {
       return $fallback;
     }
 
-    $collection_width = $collection['width'];
-    $collection_height = $collection['height'];
+    $collection_width = isset($collection['width']) ? $collection['width'] : 0;
+    $collection_height = isset($collection['height']) ? $collection['height'] : 0;
+
+    if ($collection_width == 0) {
+       $collection_width = 24; // fallback padrão
+    }
+    if ($collection_height == 0) {
+       $collection_height = 24; // fallback padrão
+    }
+
     $icon_width = $collection['icons'][$icon_name]['width'] ?? $collection_width;
     $normalized_width = round(($icon_width / $collection_width) * 100) / 100 . 'em';
 
